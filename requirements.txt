# KZ Downloader — Python dependencies
# =====================================
# Run this once to install everything:
#   pip install -r requirements.txt
#
# Then run this once to set up the Playwright browser:
#   playwright install chromium
#
# ─────────────────────────────────────
# FFmpeg (required — install separately)
# ─────────────────────────────────────
# FFmpeg is not a Python package and must be installed on your system.
#
# Windows:   winget install ffmpeg
#            (or download manually from https://ffmpeg.org/download.html)
#
# macOS:     brew install ffmpeg
#
# Linux:     sudo apt install ffmpeg -y
#
# Android:   pkg install ffmpeg
#
# After install, confirm it works: ffmpeg -version
# See the Setup Guide tab in the app for detailed instructions.
# ─────────────────────────────────────

yt-dlp
playwright
